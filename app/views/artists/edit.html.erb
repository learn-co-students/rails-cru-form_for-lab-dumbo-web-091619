<h1>Editing Artist</h1>

<%= form_for(@artist) do |f| %>
    <%= f.text_field :name %>
    <%= f.text_field :bio %>
    <%= f.submit %>
<% end %>

<%= link_to 'Show', @artist %> |
<%= link_to 'Back', artists_path %>
